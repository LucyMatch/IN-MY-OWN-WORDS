# Copy this file to `.env` and fill in your key.
# The server reads ANTHROPIC_API_KEY at startup.
# Without it, all AI endpoints return a friendly 501 with a canned message
# so the UI is still navigable for design work.

ANTHROPIC_API_KEY=

# Optional: override the default port (3001)
# PORT=3001
